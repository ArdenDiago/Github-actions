name: running an instance

on:
  - workflow_dispatch


jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: display  message
        run: echo "Hello to this world"
      - name: get systeme info
        run: echo "${{github.event_name}}"
      - name: Github ID
        run: echo "${{github.id}}"
      - name: id
        run: echo "${{github.type}}"
    