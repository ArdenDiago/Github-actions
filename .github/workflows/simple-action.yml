name: Simple action
on: [push]

jobs:
    simple-action:
        runs-on: ubuntu-latest
        steps:
            - name: simple JS Action
              id: greet
              uses: actions/hell0-world-javascript-action@v1.1.3
              with:
                who-to-greet: Arden
            
            - name: Log Greeting time
              run: echo "${{steps.greet.outputs.time}}"