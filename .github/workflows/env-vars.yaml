name: Env Vars

on: 
  - push

jobs:
    env-var-and-context:
        runs-on: ubuntu-latest
        steps:
            - name: logs env var
              run: echo $GITHUB_REF 
            - name: log context
              run: echo '${{ github.ref }}'