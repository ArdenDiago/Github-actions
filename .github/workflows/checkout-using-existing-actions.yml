name: Using the checkout feature

on:
    - push

jobs:
    checkout-action:
        runs-on: ubuntu-latest
        steps:
            - name: List Files Before
              run: ls -a
            - uses: actions/checkout@v3
            - name: List files after
              run: ls -a