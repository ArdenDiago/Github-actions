name: Event Filters - Activity Types

on: workflow_dispatch
    # pull_request:
    #     types: [opened, synchronize]
    #     branches: [main]

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Echo the event type
              run: echo "Running whenever a PR is opeaned or Synchronized and base branch is main"